import NavLink from "./navLink";

const TitleBar = function () {
  return (
    <div className="w-[100%] mx-4 overflow-hidden">
      <NavLink text="PIYUSH JAISWAL" path="/" />
    </div>
  );
};

export default TitleBar;
